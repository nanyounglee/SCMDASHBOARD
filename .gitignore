node_modules/
.claude/
package-lock.json
package.json

# 견적 계산기 엑셀 산출물 — 단가표가 통째로 들어 있어 GitHub Pages에 올리면
# 링크만 알면 누구나 받을 수 있다. 배포 스크립트의 `git add -A`에 딸려 들어가지 않게 제외한다.
# 필요하면 `python scripts/build_quote_xlsx.py`로 언제든 다시 만들어 개별 전달한다.
docs/패키지_견적계산기.xlsx
docs/패키지_견적계산기_cells.json

# Office 임시 잠금 파일 (문서를 열어둔 채 배포하면 git add -A에 딸려 들어간다)
~$*

# 제품별 표준원가가 들어 있는 파생 데이터 — 공개 저장소에 올리면 원가가 그대로 노출된다
scripts/quote_products.json
