# SCM 대시보드 v22 변경 내역 (2026.07.21)

팀 공유용 — 변경 1건당 1줄 요약입니다. 상세 사용법은 `docs/SCM_DASHBOARD_사용자가이드_v22.html`, 구현 로직은 `docs/SCM_DASHBOARD_로직설명_v22.html`, v21.x 세부 이력은 `docs/archive/CHANGELOG_v21.md` 참고.

v22는 **정보 구조 대개편** 버전입니다 — 기능이 늘면서 혼재됐던 사이드바를 업무 대분류 기준으로 재편하고, 공급망 관련 3개 화면을 하나로 통합했습니다. (버전 규칙: 큰 기능 개편 = 메이저 업, 버그·정교화 = 소수점 — v21.14 확정 규칙의 첫 적용)

## v22.1 구매전략 재고운영 기능 보강 (2026.07.21)
- **입고예정금액 카드 연결**: `raw_발주.csv`에서 `입하여부`가 비어 있는 미입하 발주를 읽고, `재고귀속파츠번호` 우선으로 `parts_master.csv`의 `표준원가`를 매칭해 `발주지시수량 × 표준원가`로 입고예정금액을 계산합니다. 카드 클릭 시 SKU별 상세 모달을 표시합니다.
- **신제품 재고비중 카드 신규**: `dashboard_group_summary.csv`의 `group_type=new_product`와 `dashboard_sku_snapshot.csv`의 `is_new_product`, `is_first_order` 플래그를 사용해 신제품 재고금액, 전체 대비 비중, 초도발주/재발주완료 구성, 초도 예외 SKU를 표시합니다.
- **시즌별 재고회전율 카드 신규**: `dashboard_group_summary.csv`의 `group_type=season`과 `dashboard_sku_snapshot.csv`의 `season_type`을 사용해 여름/겨울/봄가을별 재고금액, SKU 수, 1M/3M/YTD 회전율을 표시합니다. 기준일 월로 현재 시즌을 강조합니다.
- **수동 업로드 자동인식 보강**: 자동 로드에는 이미 포함돼 있던 `dashboard_sku_detail.csv`, `dashboard_sku_monthly.csv`, `raw_발주.csv`, `parts_master.csv`를 드래그 업로드해도 파일 유형을 인식하도록 `FILE_SIGNATURES`를 추가했습니다.
- **원본 CSV 헤더값 변경 여부**: 기존 헤더명 변경은 없습니다. 단, 신규 기능은 아래 헤더를 사용하므로 원천 시트에서 해당 헤더가 바뀌면 함께 공지와 코드 수정이 필요합니다: `raw_발주.csv`의 `파츠번호`, `재고귀속파츠번호`, `발주지시수량`, `입하여부`, `입하예정일`; `parts_master.csv`의 `파츠번호`, `파츠명`, `표준원가`, `굿즈카테고리`, `서브카테고리`; `dashboard_sku_snapshot.csv`의 `is_new_product`, `is_first_order`, `season_type`; `dashboard_group_summary.csv`의 `group_type`, `group_name`, `inv_amount`, `sku_count`, `turnover_1m`, `turnover_3m`, `turnover_ytd`.

## 사이드바 대분류 재편
- **8개 대분류로 재구성**: 대시보드(종합 현황) / **구매전략 · S&OP 재고운영**(품절 상세·졸업 검토·재고운영·매입 검토·시즌재고·EOQ) / **외주생산 · 발주 현황**(발주 현황·발주 진행현황) / **외주생산 · 이슈 현황**(이슈 현황) / **외주생산 · 공급망 현황**(공급망 포트폴리오·협력사 변경 검토↗) / **구매조달 · 매출 분석**(매출/마진·제품별 판매량 추이·원가 분석) / **구매조달 · KPI**(KPI 목표·트래킹) / **운영 서비스**
- **운영 서비스 그룹 신설**: 품질 현황(QMS)·하도급법 위험·견적계산기·AI 어시스턴트·리포트 생성 — 분석 화면이 아닌 "운영 서비스" 성격의 기능을 한 그룹으로 통합
- **원가 분석**을 발주 그룹에서 **매출 분석 그룹으로 이동** (매출 대비 원가율 분석이라 매출과 한 묶음)

## 공급망 포트폴리오 통합
- **공급망 포트폴리오 화면에 서브탭 3개**: ① 공급망 포트폴리오(기존 Tier·4분면·협력사별 상세) ② **협력사 · 매입 현황**(기존 별도 화면 통합 — 내부의 매입 현황/협력사 목록 서브탭 그대로 유지) ③ **협력사유형별 제품군**(굿즈코드×카테고리 / 협력사×인쇄유형 테이블 2종)
- 탭 전환 로직이 중첩 탭을 지원하도록 개선(바깥 탭 전환이 안쪽 서브탭 상태를 깨지 않음)

## 버전 관리
- 스냅샷 `scm_dashboard_v22.html` · 배포 스크립트 `deploy_v22.ps1` · 문서 4종 v22 갱신, v21 산출물은 archive/로 이동

## v22.2 (2026.07.21) — 팀 1차 배포 준비: 공지 정비 + 변경 리포트 + 유지보수 체계 + PDF 가이드
- **공지사항 파츠 변경 3분리**: 🧩 신규 파츠 / ⚙️ 파츠 졸업 / 💲 단가 변동을 각각 별도 박스·요약 칩으로 (사용자 요청 — 기존 통합 박스 폐지)
- **단가 변동 요약 표기**: "제품명 — 평균 N% 상승/인하 · 협력사"로 간결화 (구간별 상세는 스냅샷·가격 에폭에 보존), 백필 59건 재생성(혼재 17건 검토 확정분 포함), 브라우저 구형 캐시가 배포본을 덮지 않도록 병합 우선순위 수정
- **품절 제품 국문명 표기**: PT번호 접두 제거
- **변경 이력 리포트 생성 버튼** (리포트 생성 탭): 최근 30일의 제품/협력사/파츠/단가 변경을 파트별 섹션·담당자와 함께 .md로 다운로드 — 주간 리포트·회의 "변경사항" 섹션의 원천 데이터. **변경 스냅샷 아카이브(CSV_BANK) 바로가기** 버튼 추가
- **유지보수·업데이트 일정 문서** 신규(`docs/유지보수_업데이트_일정.md`): 자동/수동 갱신 주기 전체 표 + 담당자 원칙(구매전략 데이터 **김하은** / 외주생산 데이터 **육승미** / 대시보드 구조·기능·로직 **이난영**) + 유지보수 예고 템플릿(담당자·착수 시작일·종료일)
- **팀 배포용 PDF 사용 가이드** 신규(`docs/SCM_DASHBOARD_사용가이드_v22.pdf`, 5쪽): 시작하기·8개 대분류·탭별 기능·갱신 주기·담당자 안내

### v22.2 전면 감사 (로직·데이터구조 검증 — 발견 22건 중 확정 20건 전량 수정)
사용자 요청("모든 로직과 데이터구조 검증")으로 다중 에이전트 감사를 수행 — 6개 관점 탐색 + 건별 적대 검증(28 에이전트). 기각 2건 제외 확정 20건을 모두 수정했습니다.

**높음 (4건)**
- 이슈 현황 수량 탭·이슈율 매트릭스 등 5곳이 존재하지 않는 필드명 `수량이슈대응방안`을 참조(실제: `수량이슈대응방안_SCM`) → 대응방안이 항상 빈칸으로 나오던 문제 수정
- 표준단가 구간 선택기 2종(`partTierCost`·`tierPickArr`)이 0원 구간을 유효 단가로 채택 → '0 = 미기재' 규약으로 통일(0 스킵, `getCostTierValue`와 동일)
- 변경 감지 id가 live 감지 시 `|current`, 다음 주 스냅샷 재감지 시 `|주차` 로 달라져 같은 변경이 이중 기록 → live 감지에 현재 ISO 주차 라벨 사용 + (구분|대상) 40일 창 dedup 2중 방어
- 주간 자동화 `fetch_airtable_weekly.mjs`가 CSV_BANK의 파일(월간 스냅샷 등)을 폴더로 읽다 크래시(ENOTDIR) 가능 → 디렉토리만 순회 + 방어

**중간 (7건)**
- 발주 진행률이 `입하여부`에 '미입하'가 적혀 있어도 입하로 집계 → '입하완료' 포함·'미입하' 제외 규칙으로 통일
- change_log.json을 네트워크 오류로 못 읽은 세션이 로컬 캐시만으로 GitHub에 커밋해 배포 이력을 지울 수 있음 → 원격 정상 확인 시에만 커밋(404는 신규 생성으로 허용)
- 재발 이슈 뷰의 연도 헤더가 '2025/2026' 하드코딩 → 아카이브 기준 연도로 동적화
- CI(고객인지) 실패비용 월별 패널이 전년 월을 축에서 누락 / 예방·실패 집계가 당해분만 합산 → 양년 통합
- 전체 제품 발주량 표의 고객지시수량 합산 조건이 여분 계산과 달라 여분율 분모 불일치 → 동일 조건으로 통일
- 제품별 YoY 표가 검색어를 전년분에 적용하지 않아 검색 시 YoY가 왜곡 → 전년분에도 동일 필터
- `fetch_airtable_weekly.mjs`·`fetch_airtable_sources.mjs`가 기존 CSV 헤더를 동결해 Airtable 신규 필드가 영영 누락(내용 동일 판정으로 갱신 스킵까지) → 기존 순서 유지 + 신규 필드 뒤 병합, `weekly-airtable.yml`에 `fetch-depth: 0`(아카이브 주차 라벨 정확화)

**낮음 (9건)** — 원가분석 건수 표기 반올림, 전년 로드 캐시 게이트, 여분 저단가 판정 단가를 표시 단가와 통일, 여분 구간 셀 허위 '0개' 라벨 수정, 졸업 제품명 괄호 정규식 정밀화, `update_price_history.mjs`의 미기재↔값 전이 시 감지 기준 최신화 등

**기각 (2건)** — `getProcStdUnit`의 DB단가 폴백(정상 동작 판정), 신규 기재 단가의 변동 미감지(의도된 규약)

## v22.3 (2026.07.21) — 팀 피드백 4건: 기간 단위 확장 + 매출/마진 메뉴 통합 + 모달 정렬
- **협력사 매입 현황 기간 단위 해제**: v22 통합 때 포트폴리오 섹션의 "월별만" 잠금에 딸려 들어갔던 것을 수정 — 월별/분기별/기간지정 지원(선택 구간이 걸치는 세금계산서 작성월 전체 합산, 종합 현황 KPI와 동일 규칙). **주별만 잠금 유지** — 매입금액 원천이 세금계산서 월 단위 분할이라 주 단위 산출 근거가 없음(버튼에 이유 툴팁). 검증: 26년 2분기 합산이 독립 재계산과 일치(승화파트너스 167,971,244원)
- **제품별 판매량 추이 기간 단위 지원**: 주별/월별/분기별 선택 시 전체 이력을 그 단위 버킷으로 집계(주별은 최근 16주, 표 헤더·평균 라벨도 단위 연동), **기간지정은 선택 구간만 집계**(92일 이하 주 버킷, 초과 월 버킷)
- **매출/마진 분석 메뉴 삭제 → 원가 분석 "판매주문 분석" 탭으로 통합** (사용자 결정): 월별 매출·원가율·마진은 원가 분석 "월별 원가 요약"(프로젝트 결산 실측)이 담당하고, 판매주문 CSV 업로드 시에만 의미 있는 판매단가·추정마진율 분석은 하위 탭으로 이동(미업로드 시 안내 표시)
- **발주 TASK 상세 모달 헤더 정렬**: 발주현황 Top 표의 산출물/협력사/발주수량/공급가액 헤더 클릭 시 그 기준으로 정렬(숫자 내림차순·문자 가나다순 시작, 재클릭 시 방향 반전, 빈 값은 항상 뒤로, 모달 열 때 발주수량 내림차순으로 초기화)

## v22.4 (2026.07.21) — 팀 피드백 2차: 업태·인쇄 수기 컬럼 보존 + 발주 현황 재배치·드릴다운 + 협력사 목록 승격
- **업태·인쇄 수기 컬럼 자동 보존**: 진단 결과 sup.csv의 업태(146/146)·인쇄(67건) 데이터는 지워지지 않고 온전함(전달받은 GSheets 원본과 값 불일치 0건 — 원본에만 있는 121개사는 거래종료 이력분). 다만 Airtable 원천에 없는 수기 컬럼이라 **주간 자동 갱신이 sup을 동기화하기 시작하면 매주 지워질 위험**이 있어, `fetch_airtable_sources.mjs`에 수동 컬럼 보존 로직 추가 — API가 반환하지 않는 컬럼은 협력사 이름 매칭으로 기존 값을 이월(실데이터 시뮬레이션: 213개 값 전량 이월 확인). 신규 협력사의 업태·인쇄 기입만 수동으로 하면 됨
- **발주 현황 4개 서브탭 분리 배치** (사용자 요청): 전체 제품 발주량 / 여분(버퍼) 추천 / 여분 투자 효과 / 초도 발주 감지 — 한 화면 세로 나열에서 탭 전환으로
- **산출물 클릭 → 제품 상세 발주 현황 드릴다운**: 전체 제품 발주량에서 산출물 클릭 시 모달 — 발주 TASK·총수량·고객주문·현재 여분율·이슈 건수·재제작 칩 + **여분 요약**(권장여분/회·관행 여분·진단 — 여분 추천 표와 동일 산식) + **예방비용 vs 실패비용(여분 투자 효과)** + 발주 이력 30건 + 제품 이슈 이력(부족수량·대응 포함)
- **협력사 목록을 공급망 포트폴리오 첫 화면으로 승격** (사용자 요청): 포트폴리오 탭 진입 시 협력사 KPI 4장 + 협력사 목록 테이블(업태·제조·인쇄유형·검색)이 가장 먼저 표시, 변경 검토 진행 현황 카드와 Tier·4분면은 그 아래 — 협력사·매입 현황 서브탭은 매입 현황 전용으로 정리

## v22.5 (2026.07.22) — 데이터 업로드 영역 노출 버그 수정
- **데이터 업로드 바를 종합 현황 전용으로 고정**: `nav()`가 탭 전환 시 `sec-`로 시작하는 모든 영역(업로드 바 포함)을 숨긴 뒤 현재 탭만 다시 표시하는데, 업로드 바(`sec-upload`)는 이 재표시 대상에서 빠져 있어 **다른 탭에 갔다가 종합 현황으로 돌아와도 업로드 바가 계속 숨겨진 채로 남는** 버그가 있었음 → `nav()`에서 종합 현황 진입 시에만 업로드 바를 명시적으로 표시하도록 수정, 다른 탭에서는 숨김 유지
- **CSV 검증 칩(로드 완료 체크) 도 종합 현황 전용으로 수정**: 업로드 바 바로 아래 `validation-area`(각 CSV 로드 시 ✅/⚠️ + 행수를 보여주는 칩 목록)는 `sec-`로 시작하지 않는 별도 요소라 위 수정과 무관하게 모든 탭에서 계속 보였음(1차 수정 후 사용자 확인으로 발견) → `nav()`에 함께 종합 현황 전용 토글 추가, CSV 로드 시 칩을 그리는 `showValidationBanner()`도 현재 탭이 종합 현황일 때만 노출하도록 가드

## v22.6 (2026.07.22) — 이슈 현황 표 가독성 개선 (사용자 요청)
- **이슈 내용/대응 잘림 → 셀 내부 스크롤로 전환**: 전체·품질·수량·운영이슈 4개 표의 "내용"·"대응" 컬럼이 40자에서 강제로 잘리던 것(`crop()`)을 제거하고 전체 텍스트를 렌더링, 대신 `.iss-cell`(줄바꿈 허용 + `max-height:18px`(1줄 높이) + `overflow-y:auto`)로 감싸 **행 높이는 기존과 동일하게 유지**하면서 셀 안에서 마우스 스크롤로 전체 내용을 볼 수 있게 함
- **프로젝트/제품/담당자 컬럼 폭 축소 → 내용/대응에 공간 재배분**: 4개 표에 `<colgroup>`+`table-layout:fixed`로 컬럼폭을 명시(프로젝트·제품은 1줄 말줄임 `.iss-trunc`+`title` 툴팁으로 전체값 확인 가능), 내용·대응 폭을 표당 합산 45~49%까지 확대(기존엔 자동 폭 분배로 내용 컬럼이 좁게 밀림)

## v22.7 (2026.07.22) — EOQ·발주알람 데이터 소스 전환 + 검색 연동 + 3개월 기준 재계산 (사용자 요청)
- **정지된 레거시 CSV → 매일 갱신되는 집계 소스로 전환**: `renderEOQ()`가 오직 `inv_weekly.csv`·`sales_monthly.csv`만 사용했는데, 이 두 파일이 2026-07-07 이후 갱신이 멈춰 있어(다른 CSV는 매일 갱신) 파츠 목록이 최신 재고 전체를 반영하지 못하고 있었음 → 재고운영 현황·졸업 검토·품절 상세와 동일하게 `hasAggInventoryData()`가 참이면 매일 갱신되는 `dashboard_sku_snapshot.csv`(오늘자 275개 SKU) 기반 `renderEOQFromAgg()`를 우선 사용하도록 전환, 레거시 CSV만 있는 경우를 위해 기존 로직은 폴백으로 보존
- **파츠 검색이 EOQ 표에 안 먹던 문제 수정**: 검색창은 `refreshAll()`을 호출하는데 이 함수에 `renderEOQ()` 재호출이 아예 없었음(제품별 판매량 추이 페이지도 동일한 문제였음) → `refreshAll()`에 `if(CURRENT_SECTION==='eoq') renderEOQ();` 추가 + EOQ 결과를 파츠명·파츠번호로 필터링하는 로컬 검색 로직 신규 추가(공용 `matchesSearch()`는 dashboard_sku_snapshot의 영문 필드명을 인식 못해 별도 처리)
- **일평균수요를 "3개월 판매 추이" 기준으로 재계산**: 기존엔 로드된 CSV 전체 개월 평균을 연환산(12개월 근사)해서 최근 추세가 희석됐음 → `dashboard_sku_snapshot`의 `sales_3m`(최근 3개월 판매량)을 90일로 나눈 값으로 일평균수요 산출, EOQ·ROP·안전재고 공식(자체 로직은 불변)의 입력값으로 사용. 최근 3개월 판매실적이 없는 관리대상 SKU는 0나누기 방지를 위해 EOQ 산출에서 제외하되 "EOQ 산출 제외 SKU 수" KPI 카드로 몇 개가 왜 빠졌는지 투명하게 노출(조용히 사라지지 않도록)
- **조달유형 필터·컬럼 신규**: `dashboard_sku_snapshot`의 `procurement_type` 필드(국내생산/국내소싱/직소싱/중개소싱)를 표에 컬럼으로 추가하고 졸업 검토 페이지의 권장조치 칩과 동일한 패턴으로 필터 칩 추가
- **리드타임 파라미터 — 파츠별 실측치로 교체**: `EOQ_PARAMS.leadTimeDays`(전역 14일 고정)를 `parts_master.csv`의 파츠별 실제 "재고리드타임" 필드로 교체(5~50일로 파츠마다 상이, 값 없는 파츠만 기본 14일 폴백), 표에 실측/기본 여부 표기
- **파라미터 근거 명시**: 발주비용(5만원)·보관비율(20%)·안전재고(7일)는 코드·문서 어디에도 산출 근거가 없는 최초 구현 시의 범용 가정값임을 확인 — 화면 캡션에 "업계 통상 가정값, 사내 실측 산출값 아님"으로 명시(사용자 문의 대응, 값 자체는 변경하지 않음 — 실측 근거 필요 시 후속 확인)

## v22.8 (2026.07.22) — EOQ 안전재고 Z-score 통계식 전환 + 정지 CSV 2종 정리 (사용자 요청)
- **안전재고 파라미터 — 일수 근사 → 목표서비스수준 Z-score 통계식**: 기존 `안전재고 = 일평균수요 × 7일(고정)`은 v22.7에서 이미 "업계 통상 가정값" 임을 명시했던 근사치였음 → `안전재고 = Z(목표서비스수준) × 일별수요표준편차 × √리드타임`으로 교체. 목표 품절율 **1~3분기 3%↓(서비스수준 97%, Z=1.88) · 4분기 5%↓(서비스수준 95%, Z=1.645)** 확정(사용자 지정). 조회 기간(주/월/분기)의 분기를 자동 판별해 해당 Z값 적용. 수요표준편차는 그동안 자동로드만 되고 화면·산식 어디에도 쓰이지 않던 `dashboard_sku_monthly.csv`(파츠별 월별 판매수량, ~18개월 이력)에서 산출 — 월별 판매량의 표준편차를 일별로 환산(÷√30.44) 후 √리드타임을 곱함. 판매이력 2개월 미만 SKU(신제품 등)는 기존 안전재고일수(7일) 근사로 자동 폴백, 표에 "(근사)" 라벨로 구분해 투명하게 노출
- **재고 SKU 상세 모달의 "월별 판매추이" 아코디언도 같은 소스로 전환**: 감사 중 이 차트가 몰래 정지된 `sales_monthly.csv`(+ 폴백으로 `inv_weekly.csv`)에 의존하고 있던 것을 발견 — `dashboard_sku_monthly.csv` 기반 신규 조회 함수(`getSkuMonths`/`getSkuSalesQty`)로 교체, 실사용 확인 완료(agg 경로가 항상 우선이라 이 화면은 지금까지 정상 동작 중이었음)
- **정지된 CSV 2종 완전 정리**: `inv_weekly.csv`(S&OP 재고주간)·`sales_monthly.csv`(S&OP 매출월간)가 2026-07-07 이후 갱신이 멈춰 있던 것을 발견(다른 CSV는 매일 갱신) — GitHub Actions `weekly-google-sheets.yml`의 GSheets 동기화 목록에 애초에 이 두 파일이 없어 갱신이 멈춘 것으로 추정. EOQ·재고운영·품절 상세·졸업 검토는 이미 v22.7부터 `dashboard_sku_snapshot` 기준(agg)이 우선이라 정보 손실 없이 코드 참조·업로드 슬롯·FILE_SIGNATURES·자동로드 매니페스트(`AUTO_CSV_DEFAULT`, `CSV/_manifest.json`)에서 전량 제거, 저장소에서도 파일 삭제
- **`sup.csv`·`stockout_list.csv`는 정리 대상에서 제외** (검토 후 사용자 확인): 같은 날짜에 갱신이 멈춰 함께 정리 대상으로 제안됐으나, `sup.csv`(공급망_RAW, 협력사 마스터)는 재고 스냅샷과 무관한 별도 도메인이고, `stockout_list.csv`(Airtable `sales_status_history`)는 "품절 현황" 전용 탭(재고소유구분·품절사유·정확한 변경일)의 유일한 소스라 `dashboard_sku_snapshot`으로 대체 시 정보가 사라짐 — 둘 다 기존대로 유지
- **KPI 목표·트래킹의 죽은 폴백 코드 제거**: `computeKPIs()`의 `D.inv_weekly`/`D.sales_monthly`/`D.stockout_list` 기반 폴백 블록은 `dashboard_period_summary`가 항상 로드돼 있어 이미 도달 불가능한 코드였음(먼저 계산된 값이 항상 우선) — 정리 겸 제거, 탭 진입 게이트도 `dashboard_period_summary` 기준으로 수정

## v22.9 (2026.07.22) — 공급망 포트폴리오 화면 재배치 + 등록 협력사 수 불일치 수정 (사용자 요청)
- **등록/활성/Tier 협력사 수 불일치(147 vs 146 vs 168) 원인 규명 및 수정**: ① "협력사 목록"이 보여주던 147은 `sup.csv`(공급망_RAW) 맨 끝의 깨진 트레일링 행(이름 등 거의 모든 필드가 빈 값이고 `하도급계약 대상여부`만 `"대상"`으로 남은 Airtable 내보내기 잔재)을 걸러내지 않고 개수에 포함시킨 결과였음 → 이름 빈 행 제외 필터를 목록에도 통일 적용. ② Tier(4분면) 합계 168은 `computeFromRaw()`가 발주_RAW에만 이름이 남아있는 협력사(마스터에서 빠졌거나 이미 거래종료·졸업한 협력사)를 자동으로 Tier 집계에 편입시키던 로직 때문이었음 → 마스터(공급망_RAW)에 등록된 협력사만 Tier 집계 대상으로 제한. 결과: 등록·활성 필터·Tier 4분면 합계가 모두 **146**으로 일치. 조사 중 발견한 마스터 미등록 활성 협력사 16곳·거래종료 추정 7곳·테스트 더미 데이터 1건은 별도 확인 필요(코드가 임의로 마스터에 채워 넣지 않음)
- **공급망 포트폴리오 첫 화면 순서 재배치** (사용자 요청): 등록/활성/턴키/단순구매 KPI → Tier(4분면) 카드 + QCD×관계 매트릭스 + 등급 도넛 → **공통 상세 협력사 목록**(신규, 등록·활성·Tier 버튼이 모두 이 표 하나를 필터링) → 협력사 변경 검토 진행 현황 → 변경검토 리스트(C·D등급) 순
- **협력사 목록 2종을 표 1개로 통합**: 기존엔 "협력사 목록"(업태·제조유형·인쇄유형·담당자·결제조건·거래개월수)과 "협력사별 상세"(Tier·QCD·관계·점수·등급)가 별개 표였음 → Tier·점수 표를 기본 컬럼으로 삼고, 협력사명 클릭 시 펼치는 드릴다운에 업태·제조유형·인쇄유형·결제조건과 매입추이 차트를 표시하는 방식으로 통합(`buildSupplierDetail` 드릴다운에 인쇄유형·결제조건 표시 추가)

## v22.10 (2026.07.22) — KPI "A등급 비율" 불일치 수정 (사용자 제보)
- **공급망 포트폴리오는 Core 등급 A 6곳인데 KPI 목표·트래킹은 "A등급 비율 0%"로 나오던 불일치 수정**: `computeKPIs()`의 `supply_agrade` 지표가 포트폴리오 탭과 무관하게 `quarter_eval.csv` 원본의 `1Q_공급망관리등급`/`2Q_공급망관리등급` 텍스트 컬럼을 별도로 재계산하고 있었음 — 1Q 컬럼에 스프레드시트 수식 오류(`#REF!`) 13건이 섞여 있고, "1Q/2Q 등급이 다르면 점수를 평균낸다"는 규칙 때문에 2Q에 실제 A였던 9곳도 1Q 점수에 끌려 내려가 전부 B 이하로 재산정되며 최종 A등급 0곳으로 표시된 것이었음. 포트폴리오 탭의 협력사 소스 선택+등급/Tier 분류 로직을 `getEvaluatedPortfolioVendors()` 단일 함수로 추출해 `renderPortfolio()`와 `computeKPIs()`가 동일 함수를 재사용하도록 변경 — 이제 두 화면이 항상 같은 숫자를 보여줌(A등급 비율 0% → **4.1%**, 6/146과 정확히 일치)

## v22.11 (2026.07.22) — AI 어시스턴트: 제품 정의서 링크 + 리드타임·단가 기존 답변 검색 (사용자 요청)
- **제품명 인식**: AI 어시스턴트(`analyzeQ()`)에 제품명 매칭을 최상단 분기로 추가 — 쿼리에 제품명이 포함되면 `matchProductQuery()`가 처리하고, 없으면 기존 협력사/긴급/미입하/매입/담당자 로직으로 그대로 넘어감(회귀 없음, 협력사명 substring 매칭에 오탐되지 않도록 제품명 매칭을 먼저 실행)
- **제품 정의서 딥링크**: 신규 CSV 슬롯 `check_answer`(`Check_standard_Answer` 뷰)의 `Goods_ID`(Airtable 레코드ID)로 `https://airtable.com/appAbBz1Y48qhpHwz/pag4AWkMoOxzRwT7O?OIeeI={id}` 링크 자동 생성
- **리드타임/단가 문의 기존 답변 검색**: "리드타임/제작기간/납기/일정" 또는 "단가/가격/견적/원가" 키워드가 함께 있으면 `질문 - 문의유형`으로 필터링해 최신 3건(Q&A)을 표시, 없으면 해당 제품의 전체 문의 이력 최신 3건. 정렬은 `slackts`(Slack 타임스탬프, 초 단위 실수) 내림차순 — 문의일자 문자열(예: "2026년 7월 22일")은 자리수가 안 맞아 문자열 정렬 시 순서가 틀어지므로 sort 키로 쓰지 않음
- **답변 없을 때 폴백**: 확정 답변이 없으면 "확정 답변 이력이 없습니다"와 함께 과거 슬랙 질문 텍스트만 표시(딥링크 없음 — 사용자 확인, 실시간 슬랙 검색은 이 정적 사이트 구조상 별도 서버+Slack API 토큰이 필요해 보류)
- **검색 커버리지 확대**: 신규 CSV 슬롯 `check_question`(`Check_standard_Question` 뷰, 질문1~3+답변1~3 구조)을 `flattenCheckQuestion()`으로 `check_answer`와 동일한 형태로 평탄화해 함께 검색 — 실측 기준 리드타임 문의 54→107건, 단가 문의 3→6건으로 커버리지 확대. 두 CSV 모두 수동 업로드/교체 방식(주간 자동 재수집 대상 아님)
- **AI 패널 UI 갱신**: 예시 칩 3종 추가("밸류 메모큐브 리드타임 문의" 등 실데이터 기반), 안내 문구·플레이스홀더 갱신

## v22.12 (2026.07.22) — AI 제품문의 카드 버그 2건 수정 (사용자 제보 + 캡처 확인)
- **질문(Q) 텍스트가 안 보이던 색상 버그 수정**: `aiQaCard()`에서 날짜/유형 줄과 답변(A) 줄은 각각 `color:var(--text3)`/`color:var(--text2)`를 명시했는데 질문(Q) 줄만 색을 지정하지 않아, AI 패널의 기본 흰색 텍스트(`rgba(255,255,255,.9)`)를 그대로 물려받아 카드의 밝은 배경(`var(--surface2)`) 위에서 사실상 안 보였음(사용자가 "답변만 보인다"고 느낀 실제 원인 — 화면 캡처로 확인) → `color:var(--text)` 추가
- **check_answer·check_question 중복 표시 수정**: v22.11에서 "검색 커버리지 확대(54→107건)"로 기록했던 수치가 실제로는 **버그였음** — 두 CSV가 같은 슬랙 문의를 각자 소스에 중복으로 담고 있어(실측 flattenCheckQuestion() 2,883건 중 2,865건이 check_answer와 중복, 진짜 신규는 19건뿐) 같은 Q&A가 날짜만 다르게 두 번씩 표시되고 있었음 → 같은 (제품명+질문 앞 60자) 조합이면 `check_answer`를 우선하고 `check_question` 중복분은 제외(`dedupKey()`). 리드타임 107→54건, 단가 6→3건으로 원래 정확한 수치로 복귀

## v22.13 (2026.07.22) — 졸업 제품 수동 제외 + 실패비용 카드 전체금액 표기 (사용자 요청)
- **졸업 제품 공지 수동 제외 목록 신규**: "포인트업 러그(PITU)"·"웹캠 커버(WCCV)"가 실제로는 2026-06-25보다 이전에 이미 졸업했는데 goods_master.csv 졸업일이 최근 날짜로 재입력되어 최근 30일 창에 재노출되던 문제 — `MANUAL_GRAD_EXCLUDE` 코드 배열(사용자 확인 후 하드코딩, `MANUAL_VENDOR_TERMINATIONS`와 동일 패턴)로 졸업 제품 공지에서 제외. (같은 세션에서 조사 중 발견한 parts.csv 마이그레이션발 "파츠 연결 해제" 오탐 224건은 담당자가 원본 데이터를 직접 복구 중이라 이번 배포에서는 다루지 않음)
- **실패비용/예방비용 카드 3곳을 전체 금액(원 단위)으로 통일**: "여분 투자 효과"·"여분(버퍼) 추천"·이슈현황 "실패비용 현황" 카드가 각각 `fa()`(예: "577만") 또는 `faFull()`(예: "5,765,214원")을 섞어 써서, 실제로는 같은 값(당해년도·전체 제품 기준 두 카드)인데도 반올림 자리수 차이로 다른 숫자처럼 보였음(577만 vs 5,765,214원의 반올림 인상) → 세 카드 모두 `faFull()`로 통일, 카드 폭 초과 방지를 위해 `.kpi-val` 폰트 20px로 축소(기본 26px)

## v22.14 (2026.07.22) — 졸업 제품 "파츠 연결 해제" 오탐 재발 차단 (사용자 재제보)
- **v22.13 배포 후에도 227건이 계속 떴던 이유**: parts.csv 마이그레이션이 아직 진행 중인 상태에서 대시보드를 열어본 세션이 있어 "변경 이력 자동 갱신" 커밋이 추가로 발생 — `detectPartsChanges()`가 라이브로 재실행될 때마다 같은 224건의 "파츠 연결 해제" 오탐을 다시 감지해 `data/change_log.json`에 눌러앉기 때문에, JSON에서 지우는 것만으로는 근본 해결이 안 됐음(재발 확인)
- **표시 차단(즉시 조치)**: 졸업 제품 공지 박스에서 `goods_unlinked`(파츠 연결 해제) 감지분을 병합하는 로직을 임시로 비활성화 — parts.csv 마이그레이션이 끝날 때까지 이 유형은 화면에 아예 안 뜸(`product_graduated`만 표시). data/change_log.json에 오탐이 계속 쌓여도 화면에는 영향 없음
- **재발 방지 안전장치**: `detectPartsChanges()`의 굿즈 연결 해제 비교에 급감 가드 추가 — 비교 대상 스냅샷 간 파츠 행 수가 이전 대비 50% 미만으로 급감하면(스키마·소스 교체 상황) 해당 구간의 굿즈 연결 해제 감지를 건너뜀. parts.csv 마이그레이션이 완결되면 이 안전장치가 있는 채로 표시 차단만 해제하면 정상 복구됨

## v22.16 (2026.07.23) — 외주생산파트 주간리포트 기능 고도화 (버그 수정 5건)

- **품질이슈 날짜 파싱 수정 (`issueInWeek`)**: `품질이슈내용업데이트` 필드가 `"2026.7.21 11:08오전"` 형식의 datetime 문자열을 포함해 `getISOWeek()`가 NaN을 반환하던 문제 수정. `parseAnyDate()`를 경유해 날짜 부분만 추출한 뒤 `YYYY.M.D` 포맷으로 변환 후 주차를 계산하도록 변경 — W30 품질이슈가 0건으로 표시되던 증상 해소.
- **복합 카테고리 이중집계 방지 (`issuesByPrimary`)**: `이슈카테고리`가 `"수량이슈, 품질이슈"` 형식인 레코드가 `issuesByType()` (`.includes()` 기반)에 의해 수량·품질 양쪽 집계에 모두 포함되던 문제 수정. `primaryCat()` (쉼표 앞 첫 번째 토큰 기준)로 주카테고리를 결정하는 `issuesByPrimary()`를 추가해 단품/박스 건 중복 집계 해소. 4주 추이 테이블에도 동일 기준 적용.
- **협력사 품절 현황: 담당자 → 협력사명**: `MANUAL_STOCKOUT`의 `manager` 필드(개인 이름) 대신, PT코드로 `D.order`를 조회해 `(파트너)발주협력사명 텍스트`(회사명)를 우선 표시. `D.sup` 등록 협력사 목록과 교차 검증해 공급망 평가 포함 협력사를 우선 반환하도록 `supFromParts()` 구현.
- **공급망 등급 표기 → Tier 명칭**: 섹션6 등급 분포표에서 `A등급 (12점↑)` 형식을 `Core (A등급)` / `Performer (B등급)` / `Developing (C등급)` / `General (D등급)` 으로 변경 — 대시보드 포트폴리오 탭 기준 명칭과 통일.
- **섹션 5-6 재배치 검토표 자동 연동**: `VC_CHANGE_CACHE`(협력사 변경 검토 대시보드 실시간 데이터)에서 `confirmedDate`(확정일) 또는 `completedDate`(완료일)이 이번 ISO week에 해당하는 건을 자동 표시. 캐시 미로드 시 "포트폴리오 탭 열람 후 재생성" 안내 출력. 기존 수동 입력 자리 대체.
- **섹션 7: 3주 연속 이슈 협력사 (이슈 요약 포함)**: 기존 "최근 4주 중 N주 이슈 발생" 집계를 폐기하고 최근 3주 연속 이슈 발생 협력사만 🔴로 표시. 이번 주 이슈 품목명(`입고물품`)과 카테고리 요약(`수량`, `품질` 등)을 줄 끝에 함께 출력(예: `— PT1731-폴더블텀블러(수량)`).
- **섹션 7: 2주 연속과 3주 연속 중복 제거**: 3주 연속 협력사는 2주 연속 목록에서 자동 제외해 같은 협력사가 두 항목에 중복 표시되지 않도록 수정.
- **섹션 7: 이니셔티브/AX 방향 항목 제거**: 매주 "수동 입력 필요"로만 남아 실질 정보가 없던 항목 삭제.
- **원본 CSV 헤더값 변경 여부**: 변경 없음. 위 기능은 기존 `issue.csv`의 `이슈카테고리`, `품질이슈내용업데이트`, `입고물품` / `order.csv`의 `(파트너)발주협력사명 텍스트`, `수주처`, `산출물` / `quarter_eval.csv`의 `2Q_공급망관리등급` 헤더를 그대로 사용합니다.

## v22.15 (2026.07.22) — 구매전략파트 주간 재고 리포트 · EOQ 발주완료 표시
- **주간 재고 리포트 생성 버튼 신규**: 리포트 생성 화면에 "구매전략파트" 카드를 추가해 주차를 선택하고 누르면 1~5섹션이 채워진 `.md` 파일을 다운로드합니다. `.clasp` Apps Script 대시보드의 `generateWeeklyReportMd()`와 동일한 구조를 `dashboard_period_summary.csv`/`dashboard_group_summary.csv`/`dashboard_sku_snapshot.csv`/`raw_발주.csv`/`parts_master.csv` 기준으로 이식했습니다. 파일명도 같은 `YYYY_W주차_MMDD_주간보고_구매전략파트.md` 규칙을 따릅니다.
- **5번 섹션(주요 이슈·우선순위) 규칙 기반 자동 판정**: `inventory-strategy` 스킬(Claude Code)의 판단 기준 임계값 표를 JS 규칙엔진으로 이식해 연속 판매 0, 장기미회전, 판매 급감, 품절 장기화/초장기, 단일 SKU 집중, 신규 출시 부진 SKU를 자동으로 찾아 우선순위(🔴 긴급/🟠 높음/🟡 모니터링)와 함께 표로 정리합니다. LLM 호출 없이 결정론적 규칙만 적용하며, "특이사항 메모" 입력창 내용은 원문 그대로 5-1에 추가됩니다. (한계: 전주 대비 신규/변경 이슈만 거르는 판단은 지원하지 않음 — 매주 조건에 걸리는 전체 후보를 다시 보여줍니다.)
- **EOQ·발주알람(`renderEOQFromAgg()`)에 발주 완료(제작중) 표시**: "즉시 발주"/"발주 필요"로 뜨던 SKU 중 `raw_발주.csv`에 이미 미입하 건이 걸려있는 파츠는 🔵 발주 완료(제작중) 배지로 바뀌고, 발주 수량 합계·입하예정일이 함께 표시됩니다(재고귀속파츠번호 우선 매칭, 입고예정금액과 동일 판정 로직). "발주 필요 SKU"/"긴급 발주 대상" KPI 카운트도 이미 발주 완료된 건은 제외합니다. v22.7~v22.8의 조달유형 필터·리드타임 실측·Z-score 안전재고 컬럼은 그대로 유지됩니다.
- **사이드바 순서 조정**: "구매전략·S&OP 재고운영" 그룹에서 재고운영 현황을 그룹 최상단으로 이동.
- **원본 CSV 헤더값 변경 여부**: 신규 컬럼은 없습니다. 다만 위 기능들이 `raw_발주.csv`의 `발주지시수량`, `입하예정일`, `재고귀속파츠번호`, `파츠번호`, `입하여부`, `project`, `task_id`, `과업지시일자`와 `parts_master.csv`의 `출시일`, `표준원가`에 의존하므로 해당 헤더가 바뀌면 코드 수정이 필요합니다.
