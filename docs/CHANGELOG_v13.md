# SCM 대시보드 v13 변경 내역 (2026.07.14)

팀 공유용 — 변경 1건당 1줄 요약입니다. 상세 사용법은 `docs/SCM_DASHBOARD_사용자가이드_v13.html` 참고.

## 종합 현황
- 재고생산 TASK·매입금액을 별도 KPI 카드로 분리 표기 (전체 매입 대비 비중 포함)
- 기준 월 단일 선택 외에 기간 통합 선택 추가 — 26년 1~7월 통합·1~3월·4~6월·상반기 칩, KPI·차트·상세 모달 모두 기간 합산
- 모든 KPI 카드에 MoM(전월)·YoY(전년 동기) 증감 뱃지와 전월/전년 수치 병기
- 검색어(제품·협력사)가 월별 이슈 추이·이슈 유형 도넛·매입추이 월클릭 상세 모달에 반영 — 검색 건만 집계
- 이슈 상세 모달을 3단 구성으로 개편 — 이슈 추이 차트 + 제품×협력사 이슈율 매트릭스(행 클릭 드릴다운) + 유형별 개별 내역

## 발주 진행현황 (신규 탭)
- 담당자별 진행중/신규/완료(전주 대비) 요약 버튼 — 클릭 시 해당 TASK만 필터
- TASK별 진행률(%) 표시 — 입하예정일 기준 환산, 예정일 경과 시 "지연 n일" 강조
- 주간 CSV 규약: 매주 월요일 09:00 에어테이블 진행현황 뷰 → `CSV/progress_연도_W주차.csv`로 커밋하면 전주 파일과 자동 비교
- 비고(특이사항·협력사 입하현황) 저장/수정 — 최초 내용과 수정 내용을 일시와 함께 병기, 수정 이력 보존
- GitHub 연동(⚙ 버튼) 시 비고 저장 즉시 data/progress_notes.json 자동 커밋 → 1~2분 내 배포되어 팀 전체 공유
- ⚙ 설정 모달에 팀원용 토큰 발급 따라하기 가이드 내장 (Fine-grained PAT · Contents Read/Write 권한만 필요)

## 발주 현황
- 발주량 탭: 발주수량·MoM·YoY·월간발주빈도·연간발주량합 컬럼 + 헤더 클릭 정렬(발주수량/발주빈도/매입가액순), 검색창 연동, 굿즈코드·파츠유형 컬럼 제거
- Bottom 탭: 연속 미발주 개월수 표시, 발주량 적은 순 정렬 (비스포크 제외)
- 프로젝트별 매입: 선택 월 기준 매입금액 내림차순 + 행 클릭 시 산출물별 발주 상세 드릴다운
- 발주빈도 Top 20 탭 제거 (발주량 탭의 월간발주빈도 컬럼으로 통합)

## 원가 분석
- 주간 매출결산 CSV(`CSV/project_연도_W주차.csv`) 연동 — PNA 코드별 총매출 통합 × 매입원가·원가율·마진 Top 50 표

## 공급망 포트폴리오
- 평가 기준·산식을 월간리포트 정식 평가 단일 기준(2026.07 통합)으로 교체 — 반기 누적 실적, 예외·대체 결측 시 1점, 반기 발주 0건 D등급 고정, 마스터 전체(발주 0건 포함) 평가

## 협력사 현황
- 26년 발주 거래 개월수(예: 7월 기준 7/7)·활성 협력사(2개월 이상 거래) 표기, 활성 카드 클릭 시 필터
- 협력사명 클릭 시 연도별 월별 매입금액 추이 모달

## 고객인지 이슈
- 대응내용·실패비용 수기 입력 (입력 즉시 저장) + GitHub 연동 시 자동 커밋
- 실패비용 현황 카드 — 자동(재제작 취득원가)+수기 합산, YoY·월별 추이·매입금액 대비 비중

## 운영·안정성
- `archive_csv.ps1`: 주간 파일(progress_/project_)은 최신 1개만 유지 후 CSV_BANK/연도_W주차로 이동, 일반 CSV는 업로드 주차별 아카이브 (CSV_BANK도 깃 커밋 대상)
- `deploy_v13.ps1` 배포 스크립트 신규 (v12 스크립트 대체)
- 안정성 수정: HTML 속성 이스케이프 보강, 재제작비 조회 캐시(렌더 성능), 실패비용 월집계 세금계산서월 폴백, 고객인지 차트 빈 컬럼 크래시 수정
